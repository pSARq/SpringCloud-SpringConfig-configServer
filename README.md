Repositorio de ejemplo para entender como funciona config server (Spring Cloud)
Buscar en los repositorios de github porque deben de haber 3 como minimo, este es el servidor de configuracion,
debe haber otro que es el microservicio final que consume el servidor y otro repo que debe tener
los archivos de configuracion que este leera para poder exponerlos a los microservicios.